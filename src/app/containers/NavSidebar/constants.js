import constants from 'namespace-constants';

export const {
    MODAL_NONE,
    MODAL_WIZARD,
    MODAL_FIRMWARE
} = constants('widgets/macro', [
    'MODAL_NONE',
    'MODAL_WIZARD',
    'MODAL_FIRMWARE',
]);
