.options-inputgroup {
    display: flex;
    gap: 2rem;
    align-items: center;
    margin-bottom: 2rem;
    justify-content: space-between;
    margin: 2rem;

    label {
        font-size: 1.5rem;
    }

    input[type="checkbox"] {
        width: 32px;
        margin: 0 !important;
    }
}

.options-select {
    width: 100%;
    max-width: 250px;
}

.cutting-area {
    font-size: 1.5rem;

    tr {
        height: 30px;
    }

    .label, .value {
        width: 120px;
    }

    .value {
        text-align: right;
    }
}