interface NavbarLinkProps {
    href: string;
    icon: string;
    label: string;
    isActive?: boolean
}

export function NavbarLink(props: NavbarLinkProps) {
    return (
        <div>
            links
        </div>
    )
}
