export interface GRBL {
    minimized: boolean,
    panel: {
        queueReports: {
            expanded: boolean,
        },
        statusReports: {
            expanded: boolean,
        },
        modalGroups: {
            expanded: boolean,
        },
    }
};