export interface JogSpeeds {
    rapid: {
        xyStep: string,
        zStep: string,
        feedrate: string,
    },
    normal: {
        xyStep: string,
        zStep: string,
        feedrate: string,
    },
    precise: {
        xyStep: string,
        zStep: string,
        feedrate: string,
    }
};